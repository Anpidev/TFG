[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["openjdk17"]

[phases.install]
cmds = [
    "cd tfg && chmod +x mvnw",
    "cd tfg && ./mvnw clean install -DskipTests"
]

[phases.build]
cmds = [
    "cd tfg && ./mvnw package -DskipTests"
]

[start]
cmd = "java -jar /app/tfg/target/*.jar"
